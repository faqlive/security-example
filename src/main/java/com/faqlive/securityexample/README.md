# Código principal

Base package: `com.faqlive.securityexample`

Organización:
- `common/` utilidades transversales
- `config/` configuración global
- `shared/` piezas compartidas
- `features/` módulos funcionales
